<UserControl x:Class="MedicamentStore.PasswordEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedicamentStore"
             mc:Ignorable="d" 
            >
    <StackPanel Margin="20 10">
      
        <!-- Label -->
        <TextBlock Text="{Binding Label}" FontSize="13" d:Text="Nom"
      Name="Label" Margin="0 0 0 8"
     Foreground="#97A5B4"
      HorizontalAlignment="Left"
      VerticalAlignment="Center"
      FontFamily="{StaticResource OakSans-SemiBold}"
      />
        <PasswordBox Grid.Row="1" Name="NewPassword" Margin="13 0"
                     PasswordChanged="NewPassword_PasswordChanged"
                     Style="{StaticResource PasswordEntry}"/>
    </StackPanel>
</UserControl>
